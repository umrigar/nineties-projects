[ /* Input to mix-generated compiler.  Turing machine program in store. */
store(
  [q],
  [/* Turing machine program (pg. 73). */
   [
    0: if(0, 3),
    1: right,
    2: goto(0), 
    3: write(1)
   ]
  ]
)
].

